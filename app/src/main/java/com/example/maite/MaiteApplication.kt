package com.example.maite

import android.app.Application

class MaiteApplication : Application() {
    
    override fun onCreate() {
        super.onCreate()
        // 필요한 다른 초기화 코드가 있다면 여기에 추가
    }
}